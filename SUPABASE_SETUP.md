# 🍦 Supabase 連携セットアップガイド

Supabase（完全無料のクラウドデータベース）と連携することで、サーバーがスリープ・再起動しても投票データが半永久的に保持されます。

---

## 📋 手順 1: Supabase プロジェクトの作成（約1分）

1. [Supabase](https://supabase.com/) にアクセスし、ログイン（または無料登録）します。
2. ダッシュボードで **「New Project」** をクリックします。
3. 以下の項目を入力して **「Create new project」** をクリックします：
   - **Name**: `ice-election`（任意）
   - **Database Password**: 任意のパスワードを入力（メモしておいてください）
   - **Region**: `Tokyo (ap-northeast-1)`（日本推奨）
   - **Pricing Plan**: `Free`

---

## 🗄️ 手順 2: テーブルとセキュリティ設定（SQLの実行）

1. 左メニューの **「SQL Editor」** をクリックします。
2. **「New query」** をクリックし、以下の SQL をそのまま貼り付けて右下の **「Run」** を押します：

```sql
-- 1. 投票データ保存テーブルの作成
CREATE TABLE IF NOT EXISTS ice_votes (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  voter_id TEXT NOT NULL,
  product_id TEXT NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  CONSTRAINT unique_voter_product UNIQUE (voter_id, product_id)
);

-- 2. 検索・集計を高速化するインデックス
CREATE INDEX IF NOT EXISTS idx_ice_votes_voter_id ON ice_votes(voter_id);
CREATE INDEX IF NOT EXISTS idx_ice_votes_product_id ON ice_votes(product_id);

-- 3. 行レベルセキュリティ (RLS) の有効化
ALTER TABLE ice_votes ENABLE ROW LEVEL SECURITY;

-- 4. 読み取り・書き込みポリシーの設定
CREATE POLICY "Allow public select" ON ice_votes FOR SELECT USING (true);
CREATE POLICY "Allow public insert" ON ice_votes FOR INSERT WITH CHECK (true);
```

> 実行後に「Success. No rows returned」と表示されれば準備完了です！

---

## 🔑 手順 3: APIキーの確認

1. 左メニュー下部の **「Project Settings（歯車アイコン）」** -> **「API」**（または **「Data API」**）を開きます。
2. 以下の2つの値をコピーします：
   - **Project URL**（例: `https://xyzcompany.supabase.co`）
   - **Project API keys** の `anon` / `public` キー（長い英数字文字列）

---

## 🚀 手順 4: 環境変数の設定

### A. Render.com で公開している場合（本番環境）
1. [Render.com](https://dashboard.render.com/) の管理画面を開き、対象の Web Service（`ice-election`）を選択します。
2. 左メニューの **「Environment」** をクリックします。
3. **「Add Environment Variable」** で以下の2つを追加し、**「Save Changes」** をクリックします：
   - Key: `SUPABASE_URL` / Value: （コピーした Project URL）
   - Key: `SUPABASE_KEY` / Value: （コピーした anon public キー）
4. 自動的に再デプロイが走り、Supabase と接続されます！🎉

### B. ローカル（自分のPC）で動かす場合
プロジェクト直下に `.env` ファイルを作成し、以下のように記載します：

```env
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
PORT=3000
```

起動コマンド：
```bash
npm start
```

コンソールに `📡 Supabase連携モード: 有効` と表示されれば完了です！
